<?php
    $a = 10;
    $b = 8;
    if ($a == $b) {
        echo "A é igual a B";
    } else {
        echo "A é diferente de B";
    }

    // Operadores de comparação no PHP
    // $a == $b; // Igual
    // $a != $b; // Diferente
    // $a > $b; // Maior
    // $a >= $b; // Maior ou igual
    // $a < $b; // Menor
    // $a <= $b; // Menor ou igual
    // $a === $b; // Identico
    // $a !== $b; // Não identico
    // $a <=> $b; // Spaceship
    // $a <> $b; // Diferente
