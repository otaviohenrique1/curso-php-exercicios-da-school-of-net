<?php
    // Operadores de comparação no PHP
    // $a == $b; // Igual
    // $a != $b; // Diferente
    // $a > $b; // Maior
    // $a >= $b; // Maior ou igual
    // $a < $b; // Menor
    // $a <= $b; // Menor ou igual
    // $a === $b; // Identico
    // $a !== $b; // Não identico
    // $a <=> $b; // Spaceship
    // $a <> $b; // Diferente
    
    // Operadores Aritméticos no PHP
    // +$a; 	    // Identidade
    // -$a; 	    // Negação
    // $a + $b; 	// Adição
    // $a - $b; 	// Subtração
    // $a * $b; 	// Multiplicação
    // $a / $b; 	// Divisão
    // $a % $b; 	// Módulo
    // $a ** $b; 	// Exponencial

    // Operadores de Incremento/Decremento no PHP
    // ++$a; 	//Pré-incremento
    // $a++; 	//Pós-incremento
    // --$a; 	//Pré-decremento
    // $a--; 	//Pós-decremento

    // Operadores Lógicos no PHP
    // $a and $b; 	//E
    // $a or $b; 	//OU
    // $a xor $b; 	//XOR
    // ! $a; 	    //NÃO
    // $a && $b; 	//E
    // $a || $b; 	//OU

    // Operadores de Arrays no PHP
    // $a + $b; 	// União
    // $a == $b; 	// Igualdade
    // $a === $b; 	// Identidade
    // $a != $b; 	// Desigualdade
    // $a <> $b; 	// Desigualdade
    // $a !== $b; 	// Não identidade
